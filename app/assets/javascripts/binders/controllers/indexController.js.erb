'use strict';

angular.module('binderApp')
  .controller('IndexController', [
      'binders',
      'BinderSvc',
      function(binders, BinderSvc) {
        var indexCtrl = this;
        indexCtrl.binders = binders
      }
  ]);
