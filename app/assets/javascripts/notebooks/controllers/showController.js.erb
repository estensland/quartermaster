'use strict';

angular.module('notebookApp')
  .controller('ShowController', [
      'notebook',
      function(notebook) {
        var indexCtrl = this;
        indexCtrl.test = "Jakobius"
      }
  ]);
