'use strict';

angular.module('notebookApp')
  .controller('ShowController', [
      'notebook',
      function(notebook) {
        console.log(notebook)
        var showCtrl = this;
        showCtrl.test = "Jakobius"
      }
  ]);
