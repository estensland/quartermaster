'use strict';

angular.module('notebookApp')
  .controller('IndexController', [
      'notebooks',
      'NotebookSvc',
      function(notebooks, NotebookSvc) {
        var indexCtrl = this;
        indexCtrl.notebooks = notebooks
      }
  ]);
